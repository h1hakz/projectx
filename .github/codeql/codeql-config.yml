# .github/codeql/codeql-config.yml
name: "Custom CodeQL Configuration"
languages:
  - python

queries:
  - uses: security-and-quality

# Exclude the python SQL injection query id reported by CodeQL
query-filters:
  - exclude:
      id: py/sql-injection
