# .github/codeql/codeql-config.yml
name: "Custom CodeQL Configuration (exclude and include)"

paths-ignore: [
  "tests/**",
  "migrations/**",
  "docs/**"
]

query-filters:
   - exclude:
       id: py/flask-debug
   - include:
       problem.severity:
         - critical
         - high
